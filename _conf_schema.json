{
  "image_api_url": {
    "description": "绘图 API 地址",
    "type": "string",
    "default": "",
    "hint": "按服务商后台显示的入站端点原样填写完整地址，插件不会补全或改写路径。openai_chat 常见为 /v1/chat/completions；openai_images 常见为 /v1/images/generations。"
  },
  "image_api_protocol": {
    "description": "绘图接口协议",
    "type": "string",
    "default": "openai_chat",
    "options": [
      "openai_chat",
      "openai_images"
    ],
    "hint": "按服务商页面的入站协议选择。openai_chat 使用 Chat 请求体并支持参考图；openai_images 使用 /v1/images/generations 的 model + prompt 请求体，只支持文字绘图。"
  },
  "image_api_key": {
    "description": "绘图 API Key",
    "type": "string",
    "default": "",
    "hint": "只保存在 AstrBot 的插件配置中，不要写进插件源码或公开仓库。",
    "obvious_hint": true
  },
  "image_model": {
    "description": "绘图模型",
    "type": "string",
    "default": "",
    "hint": "填写绘图接口实际提供的模型名。"
  },
  "request_timeout_seconds": {
    "description": "最大等待时间（秒）",
    "type": "int",
    "default": 240,
    "hint": "每次下载参考图、优化提示词或绘图请求的最长等待时间，可填写 1 到 3600。"
  },
  "draw_retry_count": {
    "description": "绘图失败重试次数",
    "type": "int",
    "default": 0,
    "hint": "仅在绘图接口返回 502 或 524 时重试，可填写 0 到 3；重试可能重复生成或计费。"
  },
  "optimize_prompt": {
    "description": "启用提示词优化",
    "type": "bool",
    "default": false,
    "hint": "只控制 /draw 的自动优化；/youhua 会始终调用优化接口。"
  },
  "optimizer_max_prompt_length": {
    "description": "提示词优化最大长度（字）",
    "type": "int",
    "default": 50,
    "hint": "启用优化时，超过该长度会跳过优化并直接绘图；中文每字计 1，0 表示永不自动跳过。"
  },
  "optimizer_api_url": {
    "description": "优化模型 API 地址",
    "type": "string",
    "default": "",
    "hint": "供 /draw 自动优化和 /youhua 使用。优化接口始终发送 Chat 请求体，请按服务商页面显示的完整 Chat 端点原样填写；插件不会补全或改写路径。"
  },
  "optimizer_api_key": {
    "description": "优化模型 API Key（可选）",
    "type": "string",
    "default": "",
    "hint": "可以与绘图 Key 不同；本地无鉴权服务可以留空。"
  },
  "optimizer_model": {
    "description": "优化模型",
    "type": "string",
    "default": "",
    "hint": "供 /draw 自动优化和 /youhua 使用，填写优化接口支持的实际模型名。"
  }
}
